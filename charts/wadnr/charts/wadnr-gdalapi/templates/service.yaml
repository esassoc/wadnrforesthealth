apiVersion: v1
kind: Service
metadata:
  name: {{ include "gdalapi.fullname" . }}
  labels:
    {{- include "gdalapi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gdalapi.selectorLabels" . | nindent 4 }}
