apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sitkacapture.fullname" . }}-test-connection"
  labels:
    {{- include "sitkacapture.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sitkacapture.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
