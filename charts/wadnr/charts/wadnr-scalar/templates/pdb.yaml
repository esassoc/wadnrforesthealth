apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "scalar.fullname" . }}-pdb
spec:
  maxUnavailable: 0
  selector:
    matchLabels:
      {{- include "scalar.selectorLabels" . | nindent 6 }}
